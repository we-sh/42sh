#include "parser.h"

/*
**
*/

int		token_parse_inhib(t_proc *proc, t_lexer *lexer, int *i)
{
	log_trace("entering parsing token %-12s '|'", "TT_REDIR");
	(void)proc;
	(void)lexer;
	(void)i;

	return (0);
}
